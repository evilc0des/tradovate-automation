# Copilot Prompts

## Scaffold New Message Type
"Add a new v1 message type to `src/shared-schemas`, update Rust models, and include docs/example payloads in `MESSAGE_SCHEMA.md`."

## Add Risk Rule
"Implement a new NinjaTrader-side risk rule with unit tests and fail-closed logging."

## Replay Fixture
"Create a deterministic market data fixture and verify expected signal output from Rust strategy service."
